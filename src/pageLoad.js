const initialLoad = () => {

    // create nav bar
    const nav = document.createElement('div');
    nav.classList.add('nav');
    nav.innerHTML = "Restaurant Name";

    // create container

    // create pages

    // create footer

    return nav;
}

export {initialLoad};